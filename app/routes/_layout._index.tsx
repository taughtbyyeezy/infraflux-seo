export default function HomeView() {
  // The home view doesn't need its own bottom bar anymore as it's handled 
  // by the parent _layout.tsx to ensure smooth transitions between routes.
  return null;
}
